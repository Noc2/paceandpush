import { brandColors, brandName, brandTagline } from "./index";

export const swiftBrandTokens = {
  name: brandName,
  tagline: brandTagline,
  colors: brandColors,
} as const;

export const kotlinBrandTokens = {
  name: brandName,
  tagline: brandTagline,
  colors: brandColors,
} as const;

