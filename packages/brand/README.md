# @paceandpush/brand

Shared Pace & Push identity primitives.

The mark is a simple terminal prompt built around `>`, paired with a route
cursor and small metric pixels. The brand intentionally avoids mascots or
character art so the product stays close to the sparse developer-tool direction
in `docs/design-notes.md`.

